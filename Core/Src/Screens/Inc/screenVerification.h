#ifndef __SCREENVERIFICATION_H__
#define __SCREENVERIFICATION_H__

#include "ScreenManager.h"

int screenVerification_init( struct screenManager *sm  );
int screenVerification_eval(struct screenManager *sm);

#endif